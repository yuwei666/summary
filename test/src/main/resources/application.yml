server:
  port: 8878

spring:
  datasource:
    url: jdbc:mysql://127.16.1.108:3306/pacsdicom?useSSL=false&serverTimezone=UTC
    username: root
    password: Data!@Pass
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 20
      minimum-idle: 10
      idle-timeout: 600000
      max-lifetime: 1800000
      auto-commit: true
      pool-name: MyHikariPool
      connection-test-query: SELECT 1